# Core Flask dependencies
flask>=2.3.0
flask-cors>=4.0.0
flask-restx>=1.3.0
flask-compress>=1.14

# Data processing and computer vision
numpy>=1.24.0
opencv-contrib-python>=4.8.0
scipy>=1.10.0

# Data validation and serialization
pydantic>=2.0.0

# System monitoring
psutil>=5.9.0

# File handling and data export
openpyxl>=3.1.0
pandas>=2.0.0
reportlab>=4.0.0
pyarrow>=14.0.0

# Logging and configuration
pyyaml>=6.0

# Development and testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0

# Database support (used by error handling middleware and optional monitoring)
sqlalchemy>=2.0.0
# sqlite3 is built into Python

# Note: Redis and Celery removed as no background task processing is implemented
# The project uses in-memory task management and SSE for real-time updates
# Main storage is file-based, SQLAlchemy is only used for error handling and optional monitoring

# Machine learning for PR curve computation
scikit-learn>=1.3.0

# Optional: Advanced visualization (uncomment if needed)
# matplotlib>=3.7.0
# plotly>=5.15.0

# Optional: Redis for scalable SSE (EVENT_BUS=redis)
redis>=5.0.0

